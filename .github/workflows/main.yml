name: Deploy via Public API

on:
  pull_request_target:
    types: [labeled]

jobs:
  deploy-via-api:
    if: contains(github.event.pull_request.labels.*.name, 'deploy')
    runs-on: ubuntu-latest

    steps:
      # 1. Скачиваем содержимое репозитория (оно попадает в текущую папку)
      - uses: actions/checkout@v3
        with:
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          # Убедитесь, что устанавливаются все нужные библиотеки для клиента
          pip install requests pydantic pyyaml

      # 2. Запускаем runner.py
      # Теперь мы передаем точку "." (текущая директория), так как checkout распаковал курс прямо сюда.
      # PYTHONPATH=. нужен, чтобы питон видел пакет src как модуль.
      - name: Upload Course
        env:
          LMS_API_URL: ${{ secrets.LMS_API_URL }}
          LMS_API_TOKEN: ${{ secrets.LMS_API_TOKEN }}
          PYTHONPATH: .
        run: |
          echo "🚀 Sending to Public API: $LMS_API_URL"
          # Запускаем скрипт, указывая "." как путь к папке с курсом
          python src/runner.py .
